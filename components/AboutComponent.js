import React, { Component } from 'react';
import { Title, View, ScrollView } from 'react-native';

class About extends Component {
    render() {
        return (
            <ScrollView></ScrollView>
        );
    }

    static navigationOptions = {
        title: 'About Us'
    }

}



export default About;